import React from 'react'
import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'
import { useEffect, useState } from "react"
import { useRouter } from 'next/router'
import {addRaffle, getAllRaffles} from '../utils/raffles';
const dashboard = () => {
  return (
<div class="bg-slate-50	" className={styles.container}>
    <Head>
        <title>legit</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
    </Head>
    <nav><h1 class="bg-white px-10 py-5 font-bold text-4xl text-green-600">legit✅</h1></nav>
    <main class="min-h-screen">
    </main>
    <footer className={styles.footer}>
      built by johans.eth and taipeicity.eth
    </footer>
    </div>
      )
}

export default dashboard